[build]
  command = "npm install --ignore-scripts && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--ignore-scripts"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
